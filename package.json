{
  "name": "watchandbuild",
  "version": "1.0.5",
  "description": "watch your files and every change build the new files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir lol"
  },
  "author": "Aylon Cohen",
  "license": "MIT",
  "dependencies": {
    "fs": "^0.0.1-security"
  },
  "bin": {
    "watch": "./index.js"
  }
}
